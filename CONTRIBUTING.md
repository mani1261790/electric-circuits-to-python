# 改善提案

このリポジトリの `site/` は制作原本から生成した公開mirrorです。生成HTMLを直接編集するPull Requestは、次回生成時に失われるため受け付けません。

誤記、説明不足、再現できない実験、出典の問題を見つけた場合は、次を添えてIssueで知らせてください。

1. 章番号またはページURL
2. 問題のある文章や実験ID
3. 期待する説明または再現手順
4. 参照資料がある場合はそのURL

第三者の著作物そのものは添付せず、公開可能な参照先を示してください。
